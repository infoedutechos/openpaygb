/**
 * Shared TypeScript types for the app (URA-style `types/` folder).
 * Prefer Prisma-generated types for DB-backed enums and models.
 */
export type {
  AdminRole,
  OrganizationTenantStatus,
  PaymentRail,
  PaymentStatus,
} from "@prisma/client";
