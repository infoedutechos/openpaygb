# Deep holistic scan — 2026-07-16

**Repo:** https://github.com/infoedutechos/ODELHUBPay · **Production:** https://odelpay.vercel.app · **Commit baseline:** post-`374081c` + this scan fix pass

Companion canvases / audits: [HOLISTIC_APP_AUDIT.md](./HOLISTIC_APP_AUDIT.md) · [APP_STATUS_AUDIT.md](./APP_STATUS_AUDIT.md) · [UI_VS_CODEBASE.md](../architecture/UI_VS_CODEBASE.md) · [BACKLOG.md](./BACKLOG.md) · [TELEGRAM_MINI_APP.md](../deployment/TELEGRAM_MINI_APP.md) · [VERCEL_ODELPAY_DEPLOY.md](../deployment/VERCEL_ODELPAY_DEPLOY.md)

---

## 1. Holistic product grades

| Area | Grade | Notes |
|------|-------|-------|
| Multi-tenant tuition + school ERP | **Production** | 12 ERP modules, `PaymentAllocation`, session scope |
| Auth (master / org_admin / student / guest) | **Production** (debt) | Dual URA `admin_session` still coexists |
| Collect rails (TON, Mbiyo, LivePay UG, Relworx, Vixon, OpenPay card) | **Partial** | Env + master toggles; LivePay non-UG stubbed |
| OPGB / Dex custodial | **Partial → ops-ready** | Phase 4 + `/admin/master/opgb-ops`; Phase 5 live send/on-chain open |
| TMA + Telegram bot | **Production** / light partial | Multi-org student link + `start=schools` fixed this pass |
| Mongo / Prisma | **Production** | Single client (`utils/prisma` re-exports `lib/prisma`); indexes added this pass |
| Docs / inventories | **Aligned after regen** | Run `npm run docs:inventory` |
| Deploy | **Live** | Crons aligned to docs (TON */5, expire hourly, dex-settle */15) |

---

## 2. UI/UX vs codebase

| Finding | Severity | Resolution |
|---------|----------|------------|
| `/admin/master/opgb-ops` missing from CSV | Medium | Regenerated inventory |
| School ERP nav hid programmes/users/profile | High | Restored in `TuitionAdminShell` |
| Site Hubs menu incomplete vs Dex hub | Low–Med | Added onramp/AMM/offramp/P2P links |
| Play Hub dead tiles (`truefalse`, `tax-trivia`) | Medium | Removed from Action Center catalog |
| Dead `StudentDashboardShell` | Medium | Deleted (unused; `StudentPortalShell` is canonical) |
| Dex convert/sell/onramp oversold capability | Medium | Honest copy + deep-links |
| ~23 Play/URA admin pages not in sidebar | High (discoverability) | Documented; intentional dual surface — direct URL / item password |

---

## 3. App synchronization

| Sync plane | Status |
|------------|--------|
| GitHub `main` ↔ production | Synced via commit + `vercel deploy --prod` |
| Docs inventories ↔ routes | Regenerated this pass |
| Seed credentials ↔ `.env.local` `SEED_*` | Documented in LOCAL_DEV |
| Payment webhooks ↔ confirm path | Wired; PSP dashboard paste still ops (`B-OPS-03`) |
| Cron schedule ↔ docs | Fixed (`vercel.json` + VERCEL_ODELPAY_DEPLOY) |

---

## 4. Gaps / bugs / robustness (fix-now applied)

| Item | Action |
|------|--------|
| Demo P2P offers in production | Gated to non-production |
| Withdraw auto-complete | Already queue + master mark paid / reject |
| Master dispute ops | Shipped `/admin/master/opgb-ops` |
| Missing Mongo indexes | Added momoReference, memo, telegramId, telegramDueReminderAt, schoolClassId, admissionNo, institutionTier |
| TMA student single-org only | Cross-org fallback in `findStudentByTelegramId` |
| TMA `start=schools` | `SchoolsBrowseScreen` |
| LivePay multi-country | Explicit UG gate at livepay-start |
| TON confirm daily only | Cron → every 5 minutes |

---

## 5. Deployment readiness

| Check | Status |
|-------|--------|
| `npm run verify` | Required before ship |
| `npm run db:push` | Required after index schema change |
| Production URL | https://odelpay.vercel.app |
| Health | Bearer `HEALTH_CHECK_SECRET` |
| Email | `RESEND_*` or `BREVO_API_KEY` |
| Crons | confirm-ton */5 · expire hourly · dex-settle */15 · telegram reminders Mon 09:00 |
| Remaining ops | PSP dashboard secrets, custody for live payout |

---

## 6. MongoDB integration (no omission summary)

- **Canonical client:** `lib/prisma.ts`; `utils/prisma.ts` re-exports (game routes).
- **Schema:** `prisma/schema.prisma` only (`schema.game.prisma` archived).
- **Seed:** `scripts/seed.ts` via `npm run seed`; `prisma/seed.ts` is game-only orphan.
- **No client-component runtime Prisma imports.**
- **School ERP models ↔ `/api/admin/school/*` aligned.**
- **New indexes:** Payment `momoReference`/`memo`; Student telegram / class / admission; Organization `institutionTier`.

---

## 7. Unification (relevant only)

| Opportunity | Decision |
|-------------|----------|
| Student shells | Keep `StudentPortalShell`; deleted dead shell |
| School ERP + university programmes | Surface programmes/users/profile in school nav (same pages) |
| Org pickers | Keep `AdminWorkspaceBar` as primary; do not invent a third picker |
| Dex entry | Shared `DEX_SIDEBAR_NAV` + expanded site Hubs menu |
| Dual admin auth | Documented; do not merge URA session into tuition JWT this pass |

---

## 8. Telegram / bot / Mini App (no omission summary)

| Surface | Path | Status |
|---------|------|--------|
| Mini App UI | `/tma`, `components/tma/*` | Pay, card, receipts, admin deep-links |
| Session | `POST /api/tma/session` | initData validate + student/admin cookie |
| Bot webhook | `/api/webhooks/telegram` → `lib/telegram/flow.ts` | Keyboards + TMA launch |
| Reminders | `/api/cron/telegram-tuition-reminders` | Weekly |
| Menu button | `npm run telegram:set-menu` | Web App `/tma` |
| Multi-org link | `lib/tma-session.ts` | Prefer default org, else any telegramId |
| `start=schools` | `SchoolsBrowseScreen` | Links to `/pay` + lobbies |

**Ops:** `TELEGRAM_BOT_TOKEN`, `TELEGRAM_WEBHOOK_SECRET`, `NEXT_PUBLIC_APP_URL`, optional `TELEGRAM_ORG_SLUG`.

---

## 9. Still external (correctly)

- Hosted Visa/MC acquiring (Flutterwave/Paystack merchant)
- LivePay KE/GH/CM product enablement
- Hot-wallet on-chain OPGB delivery
- Live MoMo/bank send-money API credentials + float
