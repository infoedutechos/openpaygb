# Deep holistic scan — 2026-07-19

**Repo:** https://github.com/infoedutechos/ODELHUBPay · **Live:** https://odelpay.vercel.app  
**Related:** [APP_STATUS_AUDIT.md](./APP_STATUS_AUDIT.md) · [HOLISTIC_APP_AUDIT.md](./HOLISTIC_APP_AUDIT.md) · [BACKLOG.md](./BACKLOG.md) · [UI_VS_CODEBASE.md](../architecture/UI_VS_CODEBASE.md)

This document captures the deepest scan of app status, UI/UX vs codebase, MongoDB sync, Telegram/TMA, unification opportunities, gaps/bugs, and deployment readiness as of **2026-07-19**, plus robustness fixes shipped in the same pass.

---

## Executive verdict

| Dimension | Grade | Notes |
|-----------|-------|--------|
| Product completeness (tuition + school ERP + OPGB/Dex) | **A** | Core revenue + Help + hub hide + admin OpenPayGB |
| Mongo / Prisma money consistency | **A−** | Refund reverse + school alloc `$transaction` + idempotent confirms |
| Telegram / TMA parity | **B+** | Amount maps to real fee/installment quotes; initData dual token |
| UI/UX ↔ code alignment | **A−** | Help chrome, hub-hide dock, OpenPayGB labels; orphan Help browse removed |
| Deployment readiness | **B+** | Code-complete; **ops-gated** (PSP paste, email, `db:push`, Upstash) |
| Dirty WIP risk | **Cleared** | Admin OpenPay holder APIs included in A+ pass |

---

## Features shipped since prior audit (document these)

| Feature | Where | Docs |
|---------|--------|------|
| Hub **hide** (disappear) vs **maintenance** | MAC `#hub-visibility` / `#hub-maintenance` | Master guide |
| Visitor analytics (ecosystem totals + MAC per-page/actions) | Home strip + `#visitor-analytics` | Master guide |
| Help Center ChatGPT-style Ask anything | `/help` → `HelpCenterWorkspace` | This scan § Help |
| OpenPayGB Card in **all** school & higher admin sidebars | `TuitionAdminShell` | [OPENPAYGB_PLATFORM_CARD.md](../platform/OPENPAYGB_PLATFORM_CARD.md) |
| Admin personal OpenPayGB card | `/admin/my-card` | OpenPayGB card doc |
| Mobile back arrows on dashboards / Dex | `PageBackLink`, `DashboardMobileChrome` | — |

---

## 1. App synchronization (web ↔ data ↔ Telegram)

```
Web portals (student/admin/master)
        ↓ cookies / JWT
MongoDB (Prisma · 132 models · Pay mapped + Play unmapped)
        ↑ webhooks / confirm jobs
Payment rails (TON, Mbiyo, LivePay, Relworx, Vixon, OpenPay card)
        ↓ notify
Telegram bot DMs + TMA (/tma) + Play clicker (/clicker)
```

**Sync strengths:** payment confirm → Telegram notify; TMA session cookies share student/admin APIs; hub hide/maintenance gates public layouts.

**Sync gaps (remaining):** Play `User` vs tuition `Student` identity; orphaned bot TON menu; Play broadcast ≠ tuition students.

---

## 2. MongoDB / Prisma (no omission summary)

- **132 models** in `prisma/schema.prisma` (Mongo provider).
- Apply via **`prisma db push`** (no SQL migrations) — prod must push after schema changes (hub hide, visit paths, etc.).
- **Fixed (scan + A+ pass):** LivePay / Relworx / VixonPay / TON confirms use `updateMany({ status: "pending" })`; refunds reverse allocations + OpenPayGB card/OPGB in `$transaction`; school manual payment + allocation + on-confirm allocation in `$transaction`.
- **Still open:** missing unique on `admissionNo` / `schoolPayCode` after data cleanup; sparse `withPrismaRetry` on some money paths.

---

## 3. Telegram bot + Mini App

| Surface | Entry | Identity |
|---------|-------|----------|
| Tuition TMA | `/tma` | Student/Admin cookies after initData |
| Play clicker | `/clicker` | `User.telegramId` + per-request initData |
| Bot | webhook `/api/webhooks/telegram` | `Student.telegramId` |

**Fixed:** initData validation uses `resolvedBotToken()` (`BOT_TOKEN` **or** `TELEGRAM_BOT_TOKEN`); TMA amount snaps to installment or full-period fee quotes (rejects decorative/custom amounts).

**Still open:** webhook script may require `TELEGRAM_BOT_TOKEN` only; TMA Reports → university path for schools.

Docs: [TELEGRAM_MINI_APP.md](../deployment/TELEGRAM_MINI_APP.md), [TELEGRAM_BOT_DEPLOYMENT.md](../deployment/TELEGRAM_BOT_DEPLOYMENT.md).

---

## 4. UI / UX vs codebase

| Issue | Resolution |
|-------|------------|
| `/help` still showed FAQ browse | Wired `HelpCenterWorkspace` |
| Help + global header/footer/copilot clash | Hide header, site chrome nav, ShareFab, Platform Assist on `/help` |
| Article “← All help articles” → chat home | → `/help?view=library` |
| School ERP mobile back → `/admin` loop | Back → `/admin/school-dashboard` when school ERP |
| “Virtual card” vs OpenPayGB labeling | Student nav + card page + registry eyebrow aligned |
| Orphan `HelpCenterBrowse.tsx` | **Deleted** |
| Unused `universityOnly` flag | **Removed** (school ERP has its own nav) |
| Tuition dock ignores hub hide | **Fixed** — hide lobby/pay/receipt/admin when `hubHidden.tuition` |

---

## 5. Unification opportunities (relevant only)

1. **OpenPayGB naming:** single label helper from `lib/open-pay-brand.ts` (mostly done).
2. **Payment confirm:** one CAS pattern for all rails (**done**).
3. **Cron auth:** all crons use `requireCronAuth` (**dex-settle done**).
4. **Do not unify** URAPearls `/admin/cards` with OpenPayGB `/admin/virtual-cards`.

---

## 6. Deployment readiness checklist

1. Clean commit of interdependent WIP (**admin OpenPay included**).
2. `npm run verify` && `npm run build`.
3. Production `npm run db:push` (hub hide, visit path/action models) — **ops gate**.
4. PSP dashboard secrets = Vercel (`webhooks:alignment-check`) — **ops gate**.
5. Email keys (Resend/Brevo) + real `ODELHUB_TON_WALLET_ADDRESS` — **ops gate**.
6. Prefer Upstash (`UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN`) for distributed rate limits under load — **ops gate**.
7. Smoke: MoMo/LivePay confirm, refund allocation reverse, hub hide, `/help`, visitor path table, admin `/admin/my-card`, TMA fee quote pay.

---

## 7. Robustness fixes (scan + A+ pass)

| Fix | Path(s) |
|-----|---------|
| Idempotent payment confirm (LivePay/Relworx/Vixon/TON) | `lib/livepay|relworx|vixonpay/confirm-payment.ts`, `lib/ton/run-confirm-job.ts` |
| MoMo webhook timing-safe secret compare | `app/api/webhooks/momo/route.ts` |
| dex-settle uses `requireCronAuth` | `app/api/cron/dex-settle/route.ts` |
| Telegram initData token resolution | `utils/server-checks.ts` |
| Help chrome isolation + article back | ConditionalSiteHeader, site-chrome-nav, ShareFab, help slug |
| School dashboard mobile back | `TuitionAdminShell.tsx` |
| OpenPayGB labeling | Student shell/card page, registry panel |
| Refund reverses allocations + OpenPayGB | `lib/refund-payment.ts` |
| School alloc + manual in `$transaction` | `lib/school-payment-allocation.ts`, `lib/school-manual-payment.ts` |
| TMA amount → real fee quotes | `components/tma/TmaPayFlow.tsx`, `lib/tma-session.ts` |
| Hub-hide tuition dock | `TuitionHubBottomNav.tsx` |
| Admin OpenPay holder API | `lib/admin-openpay-api.ts`, `lib/admin-openpay-holder.ts` |

---

## 8. Ranked remaining (post A+)

1. Unique indexes for admissionNo / schoolPayCode after data cleanup.
2. Expand `apiErrorResponse` on auth + Mbiyo/MoMo/Telegram webhook outer catches.
3. Ops: PSP paste, email, production `db:push`, Upstash.
4. TMA school Reports deep-link parity.
5. LivePay multi-country + card acquiring (product).
